/*!
*
* ProfeC.net Photo Gallery Development JavaScripts
* Generated: 2014-07-23 @ 20:09:09
*
*/
$(document).foundation(), // Justified Images
$(window).load(function() {
    // collect
    bestHeight = 200, images = $("#gallery img").toArray(), maxWidth = $("#content").width(), 
    // resize
    justifiedImageRows(bestHeight, images, maxWidth);
});

// Freewall
var wall = new freewall("#free-wall");

wall.reset({
    selector: ".brick",
    animate: !1,
    cache: !1,
    cellW: 250,
    cellH: 250,
    fixSize: null,
    gutterY: 15,
    gutterX: 15,
    onResize: function() {
        wall.fitWidth();
    }
}), // wall.container.find('.brick img').load(function() {
//     wall.fitWidth();
// });
// usage: log('inside coolFunc', this, arguments);
// paulirish.com/2009/log-a-lightweight-wrapper-for-consolelog/
window.log = function() {
    if (log.history = log.history || [], log.history.push(arguments), this.console) {
        var newarr, args = arguments;
        args.callee = args.callee.caller, newarr = [].slice.call(args), "object" == typeof console.log ? log.apply.call(console.log, console, newarr) : console.log.apply(console, newarr);
    }
}, // make it safe to use console.log always
function(a) {
    function b() {}
    for (var d, c = "assert,count,debug,dir,dirxml,error,exception,group,groupCollapsed,groupEnd,info,log,markTimeline,profile,profileEnd,time,timeEnd,trace,warn".split(","); d = c.pop(); ) {
        a[d] = a[d] || b;
    }
}(function() {
    try {
        return console.log(), window.console;
    } catch (a) {
        return window.console = {};
    }
}());

// place any jQuery/helper plugins in here, instead of separate, slower script files.
// Start Google Analytics
var _gaq = _gaq || [];

_gaq.push([ "_setAccount", "UA-524508-1" ]), _gaq.push([ "_trackPageview" ]), function() {
    var ga = document.createElement("script");
    ga.type = "text/javascript", ga.async = !0, ga.src = ("https:" == document.location.protocol ? "https://ssl" : "http://www") + ".google-analytics.com/ga.js";
    var s = document.getElementsByTagName("script")[0];
    s.parentNode.insertBefore(ga, s);
}();